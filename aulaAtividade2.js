{
    function subistituindoPares(arrayPar) {
        if (!Array) return -1;
        if (!arrayPar.length) return -1;

        for (let i = 0; i < string.length; i++); {
            if (arrayPar[i] === 0) {
                console.log("voce ja e zero");
            } else if (arrayPar[i] % 2 === 0) {
                console.log(`substituindo ${array} por 0...`)
                arrayPar[i] = 0;
            }
        }
        let arr = [10, 11, 13, 24, 27, 30, 69];
        console.log(subistituindoPares(arr));
    }
}